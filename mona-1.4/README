Mona 1.4 (vendored; http://www.brics.dk/mona/)

Build just the mona binary HIP uses:

    make

Produces bin/mona. Sources are unmodified; the upstream autotools
build (configure.ac, m4/, config/) was dropped in favor of a static
Makefile with a committed config.h.
