(library
  (public_name hipsleek)
  (libraries hipsleek.common hipsleek.cil ocamlgraph))

(ocamllex
 (modules ilexer fixlexer))
; exclexer

(menhir
 (modules fixparser))

(ocamlyacc
 (modules iparser))

; run-oc new-mona test-oo


(env
 (dev
  (flags
   (:standard -warn-error -A))))
